using System.Collections;
using System.Collections.Generic;
using UnityEngine;using UnityEngine.UI; using UnityEngine.SceneManagement;

public class keizoku : MonoBehaviour
{
    // [SerializeField] public static Slider BGMslider;

    
    

   /* void Awake()
    {
        GameObject[] DontDestroys = GameObject.FindGameObjectsWithTag("DontDestroy");
        int num = DontDestroys.Length;

        if (num > 1)
        {
            Destroy(gameObject);
        }
        else
        {
            DontDestroyOnLoad(gameObject);
        }

        DontDestroyOnLoad(this.gameObject);
    }
    */
    // Start is called before the first frame update
    void Start()
    {
    }   


    //private void Awake() { DontDestroyOnLoad(this); }

    // Update is called once per frame
    void Update()
    {
        
    }
}
